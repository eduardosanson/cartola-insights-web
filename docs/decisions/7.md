# Log de Decisões — Issue #7

## DOR → SPEC — 2026-09-19

- Decisão: execução autônoma; sem perguntas, ambiguidades resolvidas aqui.
- Decisão: branch existente `eduardosanson/7-testes-automatizados-de` (criada pelo orca) mantida em vez de `feature/7`.

## SPEC → TDD/BUILD — 2026-09-19

- Decisão: axe rodou com `color-contrast` desligado (não funciona em jsdom); contraste é validado por `contraste.test.ts` calculando a razão WCAG sobre os tokens de `theme.css` nos temas claro e escuro.
- Decisão: violação real do axe (`aria-prohibited-attr` serious) nos vértices do `PentagonoQualidade` corrigida com `role="img"` em cada `<circle>` (mantém `aria-label` por eixo e o `role="img"` do SVG exigido pela issue).
- Decisão: `SplitBars` ganhou `role="group"` + `role="meter"` (com `aria-valuenow/min/max`) em vez de `role="img"`, porque expõe o valor numérico a leitores de tela.

## TDD → BUILD — 2026-09-19

- Decisão: `--accent-away` do tema claro escurecido de `#b5761e` (3,51:1) para `#966016` (4,91:1 sobre `--bg`) — reprovava 4.5:1 em texto; o escuro já passava. Mudança visual sutil aceita.
- Decisão: estados loading/vazio ganharam `role="status"`; `Tabela` recebeu estado vazio ("Nenhum clube encontrado.") que não existia (renderizava tabela sem linhas).
- Decisão: `outline:none` em `:focus` removia o foco visível (busca, linhas de modal, opções de dropdown, vértices do pentágono) — adicionado outline/stroke; hover separado de focus onde compartilhavam regra.
- Risco aceito: teste de foco visível é estático (parse do CSS), pois jsdom não computa estilos; teclado Tab/Enter/Espaço é verificado em comportamento real com `userEvent`.
- Risco aceito (débito): contraste só cobre pares texto/fundo dos tokens principais; cores derivadas via `color-mix` (chips/badges tintados) não são calculadas.
