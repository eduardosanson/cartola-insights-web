# Log de Decisões — Issue #39

_Rodada e Horário da Última Sincronização_

---

## DOR → SPEC → PROMPT PLAN — 2026-09-26

- Decisão: Usar type-only import para `SyncStatus` em componente (TypeScript `verbatimModuleSyntax`) — evita referência de valor desnecessária
- Decisão: Estado 'error' e 'null' usando string literal union type (`SyncStatus | null | 'error'`) para simplicidade — evita enum ou classe adicional
- Decisão: Integração em ambas páginas com componente único reutilizável — reduz duplicação de lógica de fetch e tratamento de erros
- Risco aceito: Se backend `/dados/status` não estiver disponível durante execução, componente exibe "Atualização indisponível" (graceful fallback) — aceitável, fluxo principal não é bloqueado

## TDD → BUILD — 2026-09-26

- Decisão: Usar `Intl.DateTimeFormat` com timezone 'America/Sao_Paulo' para respeitar fuso do usuário — mantém valor UTC em `title` para acessibilidade
- Decisão: `useEffect` sem cleanup (lista de dependências vazia) — fetch de status é feito uma única vez ao montar o componente
- Decisão: Testes primeiro com mocks explícitos de `fetchSyncStatus` — validam 3 estados (sucesso, null, erro) de forma isolada
- Risco aceito: Componente não refetch automaticamente — frontend confia que dados exibidos em Jogadores/Escalador são atualizados por refresh da página

## BUILD → EVIDÊNCIAS — 2026-09-26

- Decisão: Todas as 4 tasks completadas com sucesso: API client, componente, integração Jogadores, integração Escalador
- Decisão: Corrigido teste pré-existente em Nav.test.tsx (await screen.findByText em vez de getByText) — desbloqueou pre-commit hooks
- Decisão: Coverage mantido em 99.9% statements, 97.3% branches, 100% functions — nenhuma linha sem teste
- Risco aceito: Nenhum

## EVIDÊNCIAS → PR — 2026-09-26

- Decisão: Branch mantida como-está (`eduardosanson/39-indicar-rodada-e`) — rastreabilidade no poller
- Decisão: PR será linkado com `Closes #39` no corpo — GitHub fecha issue automaticamente ao mergear
- Status: Pronto para push e abertura de PR
