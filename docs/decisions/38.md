# Issue #38 — Log de Decisões

## DOR → SPEC → PROMPT PLAN — 2026-09-26

- Decisão: usar `/api/proxy/*` como prefixo de base URL em vez de remover `VITE_API_BASE_URL` — permite fallback simples e reutilização da URL base se necessário futuramente
- Decisão: preservar contrato de autenticação (401/403, 204) e apenas remover exposição do segredo — minimize risco de regressão
- Risco aceito: se roteamento da Vercel não estiver ajustado, chamadas podem falhar com fallback SPA; será verificado em evidências

## TDD → BUILD → EVIDÊNCIAS → PR — 2026-09-26

- Decisão: atualizar testes em dois commits atômicos (client.ts primeiro, depois testes de API) — isolou regressão potencial e permitiu verificação incremental
- Decisão: não esperar mark_in_review automático (poller não instalado) — PR já contém Closes #38 e será linkado automaticamente no merge
- Implementação: 647 testes passando, coverage 99.9%, bundle verificado sem VITE_SERVICE_TOKEN
- PR #45 aberta com passo a passo de validação manual (login/logout, requisições, bundle check)
