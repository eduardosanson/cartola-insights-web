VITE_API_BASE_URL=http://localhost:8000

# Opcional. Só necessária em ambientes que exigem o contrato X-Service-Token
# do backend (ex.: E2E isolado, staging). NÃO definir em produção — o valor
# fica em texto plano no bundle público do Vite (ver issue #18).
VITE_SERVICE_TOKEN=
