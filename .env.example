# Proxy server-side (api/proxy) e desenvolvimento local (Vite proxy)
# Variáveis SOMENTE de servidor / dev server (sem prefixo VITE_) — nunca expostas no bundle do navegador
BACKEND_ORIGIN=http://localhost:8000
SERVICE_TOKEN=

